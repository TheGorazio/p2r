import { P2rDirective } from './p2r.directive';

describe('P2rDirective', () => {
  it('should create an instance', () => {
    const directive = new P2rDirective();
    expect(directive).toBeTruthy();
  });
});
